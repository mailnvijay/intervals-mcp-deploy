services:
  - type: web
    name: intervals-mcp
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: intervals-icu-mcp --transport http --host 0.0.0.0 --port $PORT
